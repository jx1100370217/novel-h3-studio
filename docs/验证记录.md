# 本机验证记录

验证日期：2026-09-14。本记录不代表全书成片验收。

## 开始制作与音频理解后的更新

第三镜振明入山已生成并完成画面抽查和本地声音分析。三镜粗剪 384 帧、16 秒，仍为待审片段；镜头连接尚需时间字幕与洪水退去的叙事桥接。实际生成耗时见 `docs/qa/generation-speed.json`：193.423 秒、326.891 秒、197.151 秒，三镜合计 16 秒素材用时 717.465 秒。

- 已生成两镜真实 H3 连续视频，合成 260 帧、10.8333 秒、1920×1080/24 fps、48 kHz 双声道开场粗剪。原始生成分辨率 1024×576；1080p 为普通缩放，非 VOSR2。
- 第一镜采样执行约 193.5 秒。已抽查两镜画面，使用真实观察的木桩、船、山峰和金光状态续接。金光强度和运镜存在偏离，洪水未明确退去，未标记成片通过。
- 当前内置工具完成振明身份图和林间首帧；所有回执保留 actual_model=unknown / model_verified=false。当前项目允许该真实工具路径，不声称已验证 Images 2.5。
- 本地音频分析已部署：Whisper large-v3-turbo、MIT AST、Qwen2.5-Omni-7B Thinker。实际影片音轨、官方中文对白样例与合成音乐对照均实测，见 [音频验证与说明](本地音频理解.md)。
- 24 项单元与真实媒体测试通过。新增型号未知策略、视觉抽查不能代替验收、无音轨拒绝、长音频尾段覆盖、模型输出解析、视频运行时不卸载模型等检查。
- 新页面真实测试通过：通过网页提交音乐分析，确认结果、音轨读取和视频预览；没有 JavaScript 运行错误。截图 `docs/qa/audio-review.png`、`audio-music-control.png`、`opening-preview.png` 已实际查看。
- 全书逐章分析与完整改编仍未完成；当前为开场制作片段，没有完整分集或全集。参考影片完整听音、口型与电影级表演验收、VOSR2 部署仍待完成。

历史快照中的“未生成视频”和“型号阻塞”描述已被以上制作状态取代。

## 初始搭建验证（以下为开始采样前的历史快照）

- 原文件和项目原文副本 SHA-256 一致：`043025dbb6e3b6756ac4fe6e0155e838378f52dc8d804a0f0f5e3880c3e6447b`。
- GB18030 原文无损导入，318 个物理章节段、10,641 个非空段落；缺失章号为 2、3、263、264，第 315 章三段均保留。
- 318 份阅读工作单、初始角色/场景/道具档案、10 份资产图片工作单、1 份新分镜首帧任务及 1 份历史图片任务已保存。
- 本地测试 `python3 -m unittest discover -s tests -v`：**17 项通过**。包括原文完整性、证据校验、帧网格与时间偏移、跨场景续接拒绝、台词锁定、视觉注入拒绝、换图导致衍生失效、旧镜头版本失效、提交不确定时恢复取消、全书合成保护、真实 FFmpeg 混合采样率合成与尾段非静音。
- ArcReel 固定提交的**原始** `DramaNormalizedScript` / `DramaEpisodeScript` 模型和原始镜号正则校验通过。只加载模型模块，未启动 ArcReel 服务入口。
- Motion Context 的 `layout_contract.ensure()` 在本机**真实 ComfyUI** 模块上通过，非 mock。
- ComfyUI 0.34.0、RTX 5090、FL2VA / Ref2VA / Qwen3VL / 音视频 VAE 文件和所需节点核对通过，独立端口 8191 可达。
- H3 API 曾接受两镜示例的首镜任务并执行到 `SamplerCustomAdvanced`。该任务在图片型号被重新严格核实时主动停止，状态为 `canceled`，没有完成的视频产物。队列已核对为空。
- Chromium 实际页面测试：8 个页面无 JavaScript 运行错误；原文搜索保留三段第 315 章、资产显示、视觉工作单弹窗、缺图不提交、部分改编不合并全集均通过。截图已实际查看，文字、布局和状态显示正常。

页面证据：

![制作总览](/home/jx/codes/novel-h3-studio/docs/qa/overview.png)

![角色、场景与道具](/home/jx/codes/novel-h3-studio/docs/qa/assets.png)

## 尚未验证或尚未制作

- 当前内置图片工具的 Images 2.5 实际路由。工具接口无模型参数，历史图片结果未给出可核实的 2.5 型号；不能用元数据软件版本推断推理型号。
- 完成一对真实 H3 连续视频后的视觉接缝、音频接缝、角色与声线一致性、准确对白、口型和电影级表演。
- 全书逐章因果分析、正式分集、全量人物资产和完整影片。目前是 0/318 章分析、0 个正式分集、0 个验收视频；两镜技术示例不能冒充正式内容。
- VOSR2 超分部署与效果；当前只有明确的后期选型与未启用状态。
- 参考影片全片逐镜拆解及完整听音；目前只核对标题、时长和部分画面。

## 复验

```bash
cd /home/jx/codes/novel-h3-studio
python3 -m unittest discover -s tests -v
/home/jx/codes/comfyui-minimax-h3/.venv/bin/python tests/vendor_contracts.py
python3 studio.py doctor
python3 studio.py validate proof_tongtian

# 工作台已启动时，使用本机捆绑的浏览器测试依赖
PLAYWRIGHT_MODULE=/home/jx/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright node tests/ui_smoke.cjs
```

测试不会生成 Images 图片或执行 H3 采样。页面测试会尝试在缺图状态点击“生成下一镜”和在原文未完整改编时点击长片合成，以验证它们被拒绝；这份页面测试针对当前初始样例，不应在后续全量生产项目已经就绪时直接重跑。
